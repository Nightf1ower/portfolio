{
  "name": "yaroslav-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate:stickers": "node scripts/generate-stickers-manifest.mjs",
    "dev": "npm run generate:stickers && vite --host 0.0.0.0",
    "build": "npm run generate:stickers && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "postcss": "latest",
    "autoprefixer": "latest"
  }
}
